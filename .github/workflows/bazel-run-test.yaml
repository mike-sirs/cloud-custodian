name: Run test
# This workflow is triggered on pushes to the repository.
on: [push]

jobs:
  build:
    name: Run bazel test
    runs-on: l.gcr.io/google/bazel:2.1.0
    steps:
      - name: Echo the greeting's time
        run:  test --output_user_root=/tmp/output_build ...